name: TestAction1

on:
  # Triggers the workflow on pull request events but for the master branch
  push:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run the ubuntu build command
        run: echo Hello, world!
